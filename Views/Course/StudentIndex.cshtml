@model StudyTracker.ViewModels.CoursesVm

<h2>Мои курсы</h2>

<table class="table table-bordered table-hover">
    <thead class="table-light">
        <tr>
            <th>Название</th>
            <th>Описание</th>
            <th>Преподаватель</th>
            <th>Задания</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var course in Model.Courses)
        {
            <tr>
                <td>@course.Name</td>
                <td>@course.Description</td>
                <td>@course.ProfessorName</td>
                <td>
                    <a asp-controller="Assignment" asp-action="Index" asp-route-courseId="@course.Id" class="btn btn-info btn-sm">Задания</a>
                </td>
            </tr>
        }
    </tbody>
</table>
