@{
    var course = ViewBag.Course as StudyTracker.Models.Course;
    var students = ViewBag.Students as List<Microsoft.AspNetCore.Identity.IdentityUser>;
}

<h2>Подключить к курсу: @course?.Name</h2>

<form asp-action="Assign" method="post">
    <input type="hidden" name="courseId" value="@course?.Id" />
    <div class="mb-3">
        <label for="studentId" class="form-label">Выберете студента</label>
        <select name="studentId" id="studentId" class="form-select">
            <option value="">-- Студенты --</option>
            @foreach (var student in students)
            {
                <option value="@student.Id">@student.UserName</option>
            }
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Записать</button>
    <a asp-action="Index" class="btn btn-secondary ms-2">Назад к курсам</a>
</form>
